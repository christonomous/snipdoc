# PouchDB

## Console Commands

define Variable
```
var mydb = new PouchDB('todos')
```

define log-function
```
function log(x) {console.log(x)}
```

show Database entries
```
mydb.info().then(log)
```

